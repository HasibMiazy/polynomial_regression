# polynomial_regression
Polynomial Regression model using scikit-learn. Converts input into polynomial features and fits a regression curve for better prediction than linear regression. Includes data preprocessing, model training, visualization, and prediction example ( 6.5 → 158862).
